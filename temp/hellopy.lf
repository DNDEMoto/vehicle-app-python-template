target Python {
  files: [hello2.py]
}
main reactor {
    preamble {=
        import hello2
    =}
    reaction(startup) {=
        print("Hello LF (in Python).\n");
        self.hello2.test()
    =}
}